# Dashboard Header Scope Filter

## Goal
Move the dashboard scope selector from the activity card header to the page header controls and make the selected scope apply to the whole dashboard data set.

## Scope
- Reuse the existing global/user/token filter options.
- Apply the filter to overview and activity requests.
- Remove the duplicate activity-card filter action.
- Validate frontend checks and targeted backend tests.
