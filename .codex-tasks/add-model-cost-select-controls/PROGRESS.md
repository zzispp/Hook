# Progress

## 2026-06-01
- Located provider model cost modal in `apps/hook_frontend/src/sections/admin/provider-model-cost-dialog.tsx`.
- Located model picker field in `apps/hook_frontend/src/sections/admin/provider-model-cost-dialog-fields.tsx`.
- Identified admin seed translation files for new labels.
- Added checkbox rendering to each model option and dropdown-level select-all / deselect-all actions.
- Added `common.deselectAll` to Chinese and English admin seed resources.
- Ran `pnpm lint:frontend`; it passed after fixing import ordering.
