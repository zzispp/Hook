# Grouped Menu Items Table

## Goal

Menu management should display menu items grouped under their menu section so administrators can immediately see which menu belongs to which section.

## Scope

- Keep the existing menu item table actions and pagination.
- Render section header rows from database `subheader` values.
- Indent menu items under each section header.
- Preserve raw database menu values; do not translate menu section or menu item values.
