# API Menu Grouped Table

## Goal

API management should display APIs grouped by their bound menu so administrators can see which API belongs under which menu.

## Scope

- Add bound menu IDs to API permission list responses.
- Render API table with menu header rows and indented API rows.
- Place APIs without menu bindings under an explicit unbound group.
- Preserve raw database menu/API values in table rows.
