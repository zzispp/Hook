# Provider Cost Display Fix

## Goal

Fix dashboard provider cost total formatting and provider detail model cost grouping/filtering.

## Scope

- Limit dashboard provider cost distribution total display to the existing six-fraction-digit money rule.
- Group provider detail model cost rows by API key section.
- When provider model bindings restrict available models, show model costs only for the restricted binding list.
- Validate with frontend static checks where feasible.
