# Cache Monitoring Endpoint Format

## Goal

Show the actual provider endpoint API format in cache affinity monitoring so converted affinities are visible as client format to endpoint format.

## Scope

- Add endpoint API format to the cache affinity API response.
- Render converted affinities in the admin cache monitoring table.
- Run targeted validation.
