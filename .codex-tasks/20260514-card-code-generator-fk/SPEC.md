# 卡密生成者外键修复

## 目标

修复系统管理员生成卡密时 `card_codes.created_by_user_id` 外键失败的问题。

## 边界

- 系统管理员是配置虚拟用户，不强制存在于 `users` 表。
- 生成卡密仍记录生成者用户名与 IP。
- 真实数据库用户生成时继续记录 `created_by_user_id`。

## 验收

- 系统管理员生成卡密不会写入不存在的用户外键。
- Rust 编译检查通过。
