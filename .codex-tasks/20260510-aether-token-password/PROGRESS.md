# Progress

## Recovery

- 2026-05-10: Task initialized; inspecting Aether API key and password hashing rules.
- 2026-05-10: Confirmed Aether user API keys are generated by `ApiKey.generate_key()` as `sk-` plus 32 alphanumeric characters, and stored/validated with SHA256 hex hashes.
- 2026-05-10: Confirmed Aether user passwords use bcrypt via `bcrypt.hashpw(password.encode("utf-8"), bcrypt.gensalt())`.
- 2026-05-10: Hook API token generation now matches Aether key format and keeps SHA256 hex token hashes.
- 2026-05-10: Hook user password hashing now uses bcrypt, backend startup injects `BcryptPasswordHasher`, and default config admin is `admin` / `admin@example.com` with a bcrypt hash for `123456`.
- 2026-05-10: Validation passed with `cargo fmt --all`, `cargo check --workspace`, and `cargo test -p api_token -p user`.
