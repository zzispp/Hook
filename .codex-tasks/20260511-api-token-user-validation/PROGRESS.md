# Progress

## 2026-05-11

- Confirmed `UserSelect` is visually required but submit has no frontend validation.
- Confirmed `adminTokenCreatePayload` forwards empty `user_id` for `token_type === 'user'`.
- Added frontend submit validation for admin-created user tokens.
- Added field-level user selector error display and backend i18n baseline labels.
- Verified JSON parsing, TypeScript, and frontend lint.
