# Progress

## 2026-05-21

- Started from prior read-only analysis of Aether and Hook.
- Created task tracking artifacts before code changes.
- Switched Chat/CLI business format IDs to canonical `family:kind`.
- Expanded proxy conversion internals for response blocks, usage cache tokens, stream lifecycle deltas, and error conversion.
- Updated backend/frontend/test fixtures to canonical IDs and validated proxy/backend/frontend/full Rust test suites.

- 2026-05-21T13:51:41+08:00 Re-audited Aether Gemini/OpenAI/Claude directions and fixed additional block-preservation gaps: OpenAI Chat request output now preserves ThinkingBlock as reasoning_content and emits ToolResultBlock as role=tool messages; OpenAI Chat response output now preserves reasoning_content/tool_calls; Gemini response output now serializes full internal content blocks instead of flattened text. Added aether_format_alignment regression tests and split helper modules to keep files below 300 lines. Validation: cargo fmt --check, cargo check -p proxy, cargo test -p proxy, cargo test -p backend llm_proxy all passed.
- 2026-05-21T14:24:00+08:00 Re-audited Aether request conversion semantics for OpenAI/Gemini -> Claude, Claude/Gemini -> OpenAI, and OpenAI/Claude -> Gemini. Fixed confirmed remaining gaps only: OpenAI reasoning_effort now maps to internal thinking budget and back from Gemini/Claude budgets; OpenAI web_search_options now reaches Claude web_search and Gemini googleSearch; Claude output_config.effort and disable_parallel_tool_use now round-trip through internal reasoning_effort/parallel_tool_calls; Gemini thinkingConfig includeThoughts, responseMimeType/responseSchema, responseModalities, and builtin googleSearch/codeExecution are preserved through the internal model. Added focused golden tests for these paths. Validation: cargo fmt --check, cargo check -p proxy, cargo test -p proxy, cargo test -p backend llm_proxy all passed.
- 2026-05-21T14:44:00+08:00 Re-ran the six-direction Aether audit and fixed the remaining confirmed semantic deltas: InternalRequest now carries top_k and n; Claude target requests now emit Aether's default max_tokens=8192 when absent, preserve top_k, and map web_search low context to max_uses=1; OpenAI target reasoning_effort now maps xhigh budgets down to high; Gemini target requests now preserve topK and candidateCount and emit Aether-shaped functionResponse id/name/response.result for tool results. Updated golden assertions and the existing multimodal/tool-result test. Validation: cargo fmt --check, cargo check -p proxy, cargo test -p proxy, cargo test -p backend llm_proxy all passed; touched files remain under 300 lines.
- 2026-05-21T15:08:00+08:00 Final low-frequency field audit against Aether for the same six directions. Fixed confirmed remaining request-level deltas: InternalRequest now carries OpenAI sampling/logprob fields; OpenAI request normalizer preserves verbosity and extra_body.google for Gemini; Gemini request normalizer now preserves Aether native safetySettings, cachedContent, and generationConfig extra fields during Gemini round-trip, and restores google thinking_config/response_modalities. Removed an over-broad round-trip assertion for OpenAI-only sampling fields because Aether does not encode them through Gemini. Validation: cargo fmt --check, cargo check -p proxy, cargo test -p proxy, cargo test -p backend llm_proxy all passed; touched files remain under 300 lines.
- 2026-05-21T14:30:05+08:00 Re-checked Aether schema utilities and fixed the last confirmed schema-level deltas for the same six directions: OpenAI target tool schemas now add missing empty properties for object nodes, and Gemini target tool/response schemas now use Aether-style $defs/$ref flattening, allOf merge, anyOf/oneOf branch folding, unsupported-field removal, constraint-to-description hints, nullable handling, enum stringification, required alignment, and type inference. Added focused schema golden tests. Validation: cargo fmt --check, cargo check -p proxy, cargo test -p proxy, cargo test -p backend llm_proxy all passed; new schema files remain under 300 lines.
- 2026-05-21T14:44:02+08:00 Investigated real Gemini->Claude failures from local request_records. The failing Gemini history contained orphaned functionCall blocks followed by a new user turn without functionResponse. Aligned with Aether's Kiro-side principle for APIs that reject orphaned tool_use history: remove orphaned historical tool_use blocks instead of fabricating tool_result, preserve assistant text and the new user turn, and order user tool_result blocks before user text so Claude's immediate pairing rule is satisfied. Added regression tests for orphaned Gemini functionCall history and mixed text+functionResponse ordering. Validation: cargo fmt --check, cargo test -p proxy, cargo test -p backend llm_proxy all passed; latest DB records showed subsequent gemini:chat -> claude:chat requests returning 200/success or streaming.
