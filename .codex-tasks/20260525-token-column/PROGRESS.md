# Progress

- Located existing token snapshots in request_records table and RequestRecord frontend type.
- UsageRecord backend mapping already has access to snapshots; DTO/frontend type need fields.

