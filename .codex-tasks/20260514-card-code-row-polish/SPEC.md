# 卡密列表行体验修复

## 目标

修复卡密列表行的复制、类型换行和 IP 记录问题，并确认卡密状态仍使用单一状态字段表达启用、停用、已使用、已过期。

## 边界

- 不新增单独使用状态字段。
- 生成者/使用者 IP 优先使用代理头，直连时使用连接地址。
- 卡密复制使用现有 toast 与 clipboard 模式。

## 验收

- 前端 lint 通过。
- 后端编译检查通过。
