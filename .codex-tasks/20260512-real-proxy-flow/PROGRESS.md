# Progress

## 2026-05-12

- Started real proxy flow validation with user-provided local system token and provider credentials. Secrets must stay out of source files and final output.
- Added provider timeout execution in the LLM proxy path. Non-stream requests use `request_timeout_seconds`; stream/realtime paths use `stream_first_byte_timeout_seconds`. Reqwest timeout errors are recorded as `upstream_timeout` and continue through retry/failover.
- Fixed the real validation seed so the independent system token belongs to the configured admin user id instead of `null`.
- Added `.codex-tasks/20260512-real-proxy-flow/real_proxy_flow.mjs`, a secret-free runner that reads credentials from environment variables, seeds local DB rows, switches scheduling modes, calls the local proxy, and validates request candidate traces.
- Real flow results: fixed order non-stream OpenAI succeeded, fixed order stream OpenAI succeeded, retry/failover succeeded after a broken key 401, cache affinity promoted the cached key, load balance distributed across both Hook keys while keeping exact-format candidates ahead of converted candidates, OpenAI->Gemini conversion succeeded through Ekan8, Claude->OpenAI conversion succeeded through Hook, Gemini exact succeeded through Ekan8. OpenAI->Claude through AI Pai reached the provider but timed out twice at the configured 8 second provider timeout and was recorded as `upstream_timeout`.
- Validation passed: `cargo check -p backend`, `cargo test -p proxy -- --nocapture`, and `node --check .codex-tasks/20260512-real-proxy-flow/real_proxy_flow.mjs`.
- Updated the real-flow runner to use a dedicated Claude provider env pair: `CLAUDE_BASE_URL` and `CLAUDE_KEY`. The seed now points Claude to the Hook Claude provider endpoint, names the provider/key accordingly, and requires OpenAI->Claude conversion to succeed instead of accepting timeout/status failure. Added a stream conversion assertion for OpenAI->Claude and verifies the returned body is SSE.
- Reran the full real proxy flow with the new Claude provider. Results: fixed order non-stream OpenAI succeeded, fixed order stream OpenAI succeeded, retry/failover succeeded after broken-key 401, cache affinity promoted the cached key, load balance distributed over both Hook keys while keeping exact candidates first, OpenAI->Claude non-stream succeeded through `Real Test Claude Hook`, OpenAI->Claude stream succeeded through `Real Test Claude Hook`, OpenAI->Gemini succeeded through Ekan8, Claude->OpenAI succeeded through Hook Pool, and Gemini exact succeeded through Ekan8. The runner also asserted that the independent token request owner is the configured admin user id for every request.
