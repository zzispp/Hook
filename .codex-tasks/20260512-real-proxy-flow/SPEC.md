# Real Proxy Flow Validation

Validate local Hook LLM proxy scheduling, retry/failover, cache affinity, load balancing, fixed ordering, and format conversion with real provider credentials inserted into the local development database. Provider secrets are supplied at runtime only and must not be committed to source.
