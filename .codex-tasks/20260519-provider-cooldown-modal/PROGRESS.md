# Progress

- Located provider cooldown policy dialog, state, utils, types, and backend i18n seed files.

- Simplified cooldown modal to window seconds plus rule rows only.
- Verified with `pnpm lint:frontend`, `pnpm --filter hook_frontend exec tsc --noEmit`, and JSON parsing for admin i18n seeds.
