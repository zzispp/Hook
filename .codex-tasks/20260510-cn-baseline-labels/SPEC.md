# 中文 Baseline 展示值

## Goal

把开发期 baseline 里的角色名称、API 名称、菜单分组和菜单标题改成中文展示值。权限识别字段保持不变。

## Scope

- 修改 baseline role `name`。
- 修改默认 API `name`。
- 修改默认菜单 section `subheader` 和 item `title`。
- 同步前端 dashboard raw fallback 常量。
- 不修改 code/path/method/id/binding。
