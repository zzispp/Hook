export const landingStyles15ReactBitsBase01 = String.raw`
.landing-wrapper {
  background: var(--bg-body);
  color: var(--text-primary);
  font-family:
    Geist,
    var(--font-geist-sans),
    system-ui,
    -apple-system,
    BlinkMacSystemFont,
    'Segoe UI',
    sans-serif;
}

.landing-wrapper *,
.landing-wrapper *::before,
.landing-wrapper *::after {
  box-sizing: border-box;
}

.landing-wrapper a {
  font-weight: 500;
  text-decoration: none;
}
`;
